[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 5

[healthcheck]
  path = "/health"
  intervalSeconds = 30
  timeoutSeconds = 10